# hxy7 测试清单

- 已运行 `npm.cmd run typecheck`，通过。
- 已运行 `npm.cmd run build`，通过。
- 检查 `/` 会重定向到 `/login`。
- 检查登录页使用 `public/login-bg.png` 作为全屏背景，登录卡片独立叠加。
- 检查演示账号 `student`、`teacher`、`admin` 均按账号 + 密码登录。
- 检查证据上传使用真实图片 dataURL，相关列表通过 `file_url` 或资源图显示预览。
- 检查证据背包不显示批改信息，学习档案只显示 `in_portfolio` 的正式内容。
- 检查点位完成写入运行时进度，任务中心刷新后显示完成。
- 检查教师发布任务可选择多个班级，API 不再过滤为单一绑定班级。
- 检查管理端每个账号行随机重置密码，并写入可登录的新密码。
